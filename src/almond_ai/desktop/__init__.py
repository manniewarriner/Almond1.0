"""Almond Desktop: the staff-facing native Qt application.

Drives the exact same `almond_ai.core.AlmondCore` backend as the Textual
terminal -- this package contains presentation only (windows, widgets,
icons, styling, threading glue). No model/session/tool logic lives here.
"""
