- Goal: maintain and improve `firm-ai` as **Almond**, a standalone local-first Windows desktop AI for a small financial firm. The working project is `C:\Users\manni\Almond1.0`; the source remains named `firm-ai` in Google Drive/GitHub. The user wants direct answers without filler, requested automatic context compaction around 500k tokens, and explicitly prohibited subagents and background terminals.
- All eight original phases are complete: configuration/health checks, deterministic calculator, Markdown/text/PDF ingestion, local keyword retrieval with citations, fake-provider ask workflow, mocked-only HTTP adapter, fail-closed permissions, and local audit logging. Hard restrictions remain: no trading, money movement, messaging, client-record writes/deletes, arbitrary shell/SQL execution, production access, or real client data. Adversarial tests cover prompt-injection inertness, path traversal, permission leaks, and traceback leakage.
- Almond now has a native Tkinter desktop interface with official Almond Financial branding sourced from `https://www.almondfinancial.co.uk/`, including its logo and palette (`#eb5e25`, `#2b2b29`, `#363432`, `#fdfaf5`, `#d1c8bf`). Tabs are General Chat, Ask, Search, Calculator, Audit, and Health. The General Chat input was moved above the transcript and enlarged so typed text is fully visible; deterministic NSFW filtering is enabled.
- Local inference is fully connected without GPT, Claude, Anthropic, or any cloud API at runtime. Almond launches hidden `runtime\llama.cpp\llama-server.exe` (`llama.cpp b10964`) on loopback `127.0.0.1:11435` with `runtime\models\Qwen3.5-2B-Q4_K_M.gguf` (1,280,835,840 bytes), then shuts it down when the app closes. The app code belongs to the user, but Qwen weights and llama.cpp are third-party open-source components, not a proprietary model trained by the user.
- Relevant implementation files are `app\desktop.py`, `app\desktop_service.py`, `app\local_model.py`, `app\providers\llama_cpp.py`, `app\workflows\chat.py`, `app\safety\content_filter.py`, and `tests\test_llama_cpp_provider.py`. A Qwen empty-response bug was fixed by sending `"chat_template_kwargs": {"enable_thinking": false}`. Real end-to-end validation produced exactly `Almond local chat works.` after 5.44 seconds startup and 3.15 seconds response; cleanup confirmed `ModelProcesses : 0`. Automated validation: 95/95 tests pass, Ruff check passes, Ruff format check passes, 56 files formatted.
- Desktop launch is installed at `C:\Users\manni\OneDrive\Desktop\Almond.lnk`, targeting `C:\Users\manni\Almond1.0\.venv\Scripts\pythonw.exe` with arguments `-m app.desktop`, working directory `C:\Users\manni\Almond1.0`, and the official icon. Source changes were synced to Drive folder `https://drive.google.com/drive/folders/1uGFdcPl-S7o_GwrDnKfT7iU_R2p43ofU`; runtime/model binaries were intentionally excluded. Open item: `data\sample_documents\` is still a placeholder, so Ask/Search return no evidence until safe synthetic documents are added.
