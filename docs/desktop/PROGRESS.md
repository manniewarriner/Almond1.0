# Almond Desktop — Progress Log

## 2026-09-16 â€” Current UI checkpoint

- Document bot now presents one `Create PDF` button, opens Windows file explorer directly, formats the selected file with Almond branding, and changes to `Create Another PDF` after success.
- Added an in-app `PDF created` confirmation dialog showing the saved output path.
- Removed visible chat composers/history from all Desktop bot pages; specialist actions are button-only while backend sessions remain private.
- PDF formatting defaults to the healthy local `MiniCPM5-2B-Q4_K_M.gguf`; the corrupted Qwen file no longer blocks normal Desktop PDF creation.
- `launch_almond_desktop.py` now sets the project working directory before startup, ensuring `.env` and relative data paths resolve correctly.
- Verification: real PDF smoke test succeeded; `tests/test_document_ai_formatting.py` passes 17 tests. Desktop legacy test expecting stored PDF chat messages now conflicts with intentional button-only behavior.

## 2026-09-15

- Built Almond Desktop (PySide6) alongside the existing Textual terminal, sharing one `AlmondCore` backend (`src/almond_ai/core/application.py`) — one provider, one `SessionManager`, PDF/audit/permission path reused, not duplicated.
- Desktop shows General Chat + Research (Coming Soon)/Drafting/Document only; Coding stays terminal-only. Launch: `almond desktop`. (A separate session later moved the old terminal from bare `almond` to `almond dashboard` and added a new `almond` = AlmondCode CLI — `almond desktop` is untouched by that.)
- New dependency `pyside6>=6.6` added to `pyproject.toml` after explicit user approval (AGENTS.md requires approval for new deps).
- Design refinement pass (frontend-design skill): removed all ALL-CAPS labels, fixed literal `--`/em-dash copy leaks in user-facing text, gave Research's "Coming Soon" its own badge widget (`ChatView` now takes `empty_status`), defined a real 4-step type scale in `theme.py` (`TYPE_MICRO/BODY/EMPHASIS/HEADLINE`), replaced the startup splash's generic progress bar with a single breathing almond-glyph animation.
- Made a Desktop shortcut (`C:\Users\manni\OneDrive\Desktop\Almond AI.lnk`, `pythonw.exe`, `-m almond_ai.cli desktop`) with a new distinct icon (`src/almond_ai/desktop/assets/almond-desktop.ico` — same orange badge as the terminal's icon, solid glyph instead of outline) wired into the app's own window/taskbar icon too, plus a separate Windows AppUserModelID (`AlmondFinancial.Desktop`) so it doesn't group with the terminal.
- Tests: `tests/test_core_application.py` (9) + `tests/test_desktop.py` (13), all passing; ruff clean; full suite was 270/270 before the design pass, desktop-scoped subset re-verified after each later change.
- Known issues: this machine's `.venv\python.exe` spawns a duplicate child process on every launch (documented in the almondbridge skill + memory) — harmless for the GUI app, but breaks repeated `almond ctl` calls on the terminal side. Killing the GUI app via `Stop-Process` (not a normal window close) can orphan its `llama-server.exe` child since `closeEvent`-based cleanup doesn't run — not a bug, just a testing gotcha to remember.

## 2026-09-16

- Swapped the bundled local model from Qwen3.5-2B to `MiniCPM5-2B-Q4_K_M.gguf` (official `openbmb/MiniCPM5-2B-GGUF`, standard `LlamaForCausalLM` arch) — updated both config defaults (`src/almond_ai/config.py` `ModelSettings.model` and `app/config.py` `local_chat_model`), `.env.example`, and hardcoded test filenames. Verified end-to-end through the real `AlmondCore` → local llama-server path (actual generated replies, fully loopback-only).
- Concurrency fix: replaced the single global `_active_worker`/`_pdf_worker` gating in `main_window.py` with a per-bot `_active_workers` dict, so switching tabs no longer blocks or wipes another bot's in-flight reply or PDF creation; `_on_nav_selected` now skips the destructive `render_messages()` re-render whenever a worker (chat or PDF) is still active for that bot, so background completions aren't silently lost.
- Drafting bot: added a persistent "New Draft"/"Edit Last Draft" toolbar (`ChatView(toolbar=...)`, hidden until the first draft exists) — `DraftingDialog.get_prompt()` now returns `(prompt, fields)` so "Edit" can reopen the dialog pre-filled with the original structured inputs. Tightened the bot's instructions to stop it inventing status claims ("received", "processed") not present in the input, and added a persisted "⚠ Review before sending" warning that survives nav-away/back (patched directly into `core.sessions` history, not just the live label).
- New shared "thinking" indicator (`_ThinkingIndicator` in `chat_view.py`): a static bot glyph + 3 dots that colour-cycle every 350ms via a single QTimer — deliberately built with **no** `QGraphicsOpacityEffect`/`QPropertyAnimation` (both real GPU/CPU costs: offscreen alpha compositing + ~60Hz repaint) after the first version used them. Shown until the first streamed token arrives, then swapped for real text via `update_streaming_text`. Also added per-turn timing display ("Thought for Xs · replied in Ys", from `ChatStreamWorker.finished_ok(text, thinking_secs, total_secs)`).
- Document bot: added a slim native `QProgressBar` (`stepProgress`) under the reply text, driven by `PdfCreateWorker`'s existing `progress` signal via a simple text-prefix → percent map (`_pdf_progress_percent` in `main_window.py`) — deliberately skipped a "live PDF thumbnail preview" idea from a design reference since rendering thumbnails on every tick would add real cost, the opposite of the goal. PDF creation intentionally never calls the AI model (deterministic extract→render→verify, sub-second for typical files) — routing it through the model would be slower and heavier, not more efficient.
- UI polish: removed the paperclip icon from the composer (no upload feature behind it) and replaced the barely-visible 1px composer border with a clearer 2px ring (`#3A4450`); removed the sidebar's unrelated "Clarity for a brighter tomorrow" promo card; General Chat now carries the same "Coming Soon" badge as Research (sidebar row + welcome heading) — this got reverted twice by background `/code-review` and `/subtask` agents that lacked session context before landing correctly, worth remembering if it reappears.
- Fixed a real bug from the "thinking" indicator work: `begin_assistant_message(show_thinking=False)` was still constructing a full `_ThinkingIndicator` (QTimer + 4 QLabels) only to immediately discard it — wasteful on every historical-message replay (`render_messages`, fired on every tab switch). Now only constructed when `show_thinking=True`. Also enabled the thinking dots for the Document/PDF flow (previously skipped entirely), shown until the first real progress update arrives.
- Fixed Document's loading animation not matching Drafting/Chat: `_on_pdf_progress` was calling `update_streaming_text()` on every intermediate step ("Preparing document…" etc.), which hides the shared bot+ellipsis `_ThinkingIndicator` the instant the first tick lands. Now intermediate ticks just track status text for history; the thinking indicator keeps animating (same as every other bot) until `_on_pdf_finished`/`_on_pdf_failed` reveal the real terminal text.
- Removed the Document bot's step-progress bar entirely per follow-up request — it was the only caller of `ChatView.set_progress`/`QProgressBar#stepProgress`, so deleted the widget, the `_pending_progress` field, the `set_progress` method, the `_pdf_progress_percent`/`_PDF_PROGRESS_STEPS` step→percent table in `main_window.py`, and the now-dead `QProgressBar#stepProgress` theme rule. Document now shows exactly the same bot+ellipsis indicator as Drafting, no bar underneath. `tests/test_desktop.py` (24) still passing, ruff clean (one pre-existing unrelated E501 in `drafting_dialog.py`, not from this change).
- Ran `/code-review` on the whole desktop app + the shared backend it drives, fixed every real finding, verified with the full suite (323 passed) plus ruff. A background `@code-review` agent independently found and fixed the same first-round findings concurrently (confirmed via file content, no corruption from the overlap):
  - `app/local_model.py` `LocalModelServer.start()`/`astart()`: added `_start_lock` around the check-then-spawn sequence — two per-bot worker threads racing in while the model is cold could otherwise both spawn `llama-server.exe`, orphaning one.
  - `desktop/icons.py`: deduplicated `_pixmap()`/`_brand_mark_pixmap()`'s SVG-rasterization logic into one shared `_render_svg()` helper.
  - `desktop/widgets/chat_view.py`: removed dead `_last_assistant_row` field (written, never read).
  - `desktop/widgets/settings_view.py`: removed dead `"[::1]"` bracketed-IPv6 entry from `_privacy_summary()` (`url.host` never returns the bracketed form).
  - `core/application.py` `create_pdf()`: moved `tools.authorize_run()` inside the try block and added a `PermissionError` except clause that writes an audit event (`outcome="denied"`) before re-raising — a denied/disabled-tool PDF attempt was previously skipping the audit trail entirely, violating AGENTS.md's audit-logging requirement.
  - `desktop/main.py` `_StartupScreen`: added a `closeEvent` guard that ignores a titlebar-X close while `ModelStartupWorker` is still running, so `quitOnLastWindowClosed` can't tear down the event loop mid-spawn and orphan the `llama-server.exe` child before `MainWindow` (and its own finish-before-close guard) even exists.
  - `desktop/main_window.py`: a failed/blocked chat turn is rolled back out of `session.messages` by design (so it never poisons model context) but was vanishing from the visible UI on the next nav-away/back since `render_messages()` rebuilds purely from session state. Added `_bot_notices` (UI-only, never fed to the model) so the failed exchange redisplays after `render_messages()` until the bot's next successful send.
  - `desktop/main_window.py`: replaced the unbounded `_pdf_messages` list (grew one entry per PDF ever created in the session) with `_last_pdf_message` — `_regenerate()` only ever checks the tail of `session.messages`, so only the most recent PDF message can ever match; O(1) instead of unbounded.
  - One re-review finding was investigated and reverted: `document_formatting.py`'s numbered-list `numbers != [1..N]` check (skip converting to a list block if source numbering isn't sequential) looked like a bug but is intentional — `app/pdf/renderer.py` always renders ordered lists as sequential 1..N regardless of source numbering, so converting a non-sequential list would misrepresent the source; leaving it as verbatim paragraph text is correct and is exactly what `test_numbered_list_offsets_are_not_changed`/`test_zero_padded_numbering_is_preserved` assert.
  - Full suite: 323 passed (2 pre-existing, unrelated failures in `test_developer_services.py` deselected — a `FakeResponse` mock missing `__iter__` for the streaming path, not touched by any of this work).
- Live report "PDF generation failed" on the Document bot: reproduced directly against the running app's real local model + a real 1,174-page PDF. Root cause was **not** the orphaned-process race above (that instance was already clean, single server, healthy) — it's `document_formatting.py`'s intentional fail-closed validation firing for real: MiniCPM5-2B occasionally returns a block ID under the wrong category (e.g. a `heading`-only candidate reclassified as `numbered_lists`), and the code correctly aborts the whole PDF rather than trust it — this is deliberate anti-prompt-injection behavior (`test_invalid_or_injected_decisions_fail_without_modifying_source`), not a bug. Confirmed by trying to relax it and finding it breaks that security test; reverted the relaxation attempt.
- User decision: give the Document bot's AI-formatting call its own dedicated local model (`Qwen3.5-2B-Q4_K_M.gguf`, the model this feature was originally tuned against) on a second llama-server instance, while chat/drafting keep MiniCPM5-2B on the existing server. Implemented dual-server wiring:
  - `app/local_model.py`: `LocalModelServer` now takes a `port` constructor param (was hardcoded `LOCAL_MODEL_PORT`); `is_ready()` changed from `@staticmethod` to an instance method using `self.port`. Added `LOCAL_DOC_MODEL_PORT = 11436`.
  - `src/almond_ai/config.py`: `ModelSettings.document_model: str = "Qwen3.5-2B-Q4_K_M.gguf"`, plus `.env.example`/`README.md` documenting `ALMOND_DEV_MODEL__DOCUMENT_MODEL`.
  - `src/almond_ai/models/provider.py`: `LocalHTTPProvider` gained a `document_provider: LocalHTTPProvider | None` composition field; `complete_document()` delegates to it when set (falls back to `self` otherwise, unchanged for ollama/approved). `create_provider()` builds a second `LocalModelServer`+`LocalHTTPProvider` pointed at `document_model`/port 11436 whenever it differs from the chat model, and passes it in. `stop()` now stops both servers.
  - Qwen's original GGUF was found fully deleted from `runtime/models/` (removed, not kept, during the earlier MiniCPM5 swap) with no recorded source anywhere in the repo — re-downloaded `unsloth/Qwen3.5-2B-GGUF`'s `Qwen3.5-2B-Q4_K_M.gguf` (~1.28GB, matches the exact filename convention already referenced in `code_cli.py`), after confirming no official `Qwen/` org GGUF repo exists for Qwen3.5.
  - Full suite re-verified green after the plumbing changes: 323 passed, same 2 pre-existing unrelated deselects.
- **Open/next step:** the Qwen download and end-to-end verification of the dual-server Document-bot flow (actually creating a PDF against the new Qwen server) were still in progress when this session was compacted — confirm the download finished cleanly and re-run the same large-PDF repro that originally caught the MiniCPM5 misclassification, this time against the Qwen server on port 11436.

## 2026-09-16 (cont.) — Document bot fallback + desktop app review/icon fixes

- Ran a full read-only review of the whole desktop app (`main.py`, `main_window.py`, `theme.py`, `icons.py`, `workers.py`, all `widgets/*.py`) and fixed every finding: false "Coming Soon" badges on shipped features, no cross-bot concurrency guard (added `_any_worker_active()` so chat/PDF/regenerate across *different* bots can't run simultaneously against the shared provider), no startup timeout (added a 90s `_StartupScreen` timeout with a real close/cancel escape hatch instead of an unconditional-ignore titlebar X), scattered inline `setStyleSheet` calls migrated into `theme.py`'s central QSS, a `padding: 2px 2px` typo, plus comments documenting two intentionally-fragile bits (double safety-filter check, status-badge placeholder ordering).
- **Contradicts the "Open/next step" above**: tested the Document bot's AI-formatting today and `runtime\models\Qwen3.5-2B-Q4_K_M.gguf` is present but **corrupted** — llama-server itself reports `tensor 'blk.15.ffn_down.weight' data is not within the file bounds, model is corrupted or incomplete`. On-disk size is 976,476,704 bytes (~931MB), not the ~1.28GB the prior session's re-download logged above — worth checking whether that download actually completed/landed, or got overwritten/interrupted since. No backup copy found anywhere on this machine (Downloads/Documents/home searched).
- **Fix (not just diagnosis)**: `LocalHTTPProvider.complete_document()` (`src/almond_ai/models/provider.py`) now falls back to the healthy main chat provider (MiniCPM5-2B, already running) when the dedicated document-model server fails to start, instead of hard-failing every PDF. Verified for real (not mocked): `core.create_pdf(..., ai_format=True)` produced an actual non-trivial PDF via the fallback path (`formatting_method: "ai_structure"`, no warnings). Will automatically prefer Qwen again once the file is fixed — no further code change needed then.
- Also improved diagnosability for next time: `app/local_model.py`'s `LocalModelServer` now captures llama-server's stderr to a log file instead of `DEVNULL` and surfaces the real failure line in the raised error; `application.py` gained `_cause_chain()` so the full wrapped-exception chain (down to that llama.cpp detail) reaches the app's own Logs, not just the deliberately generic user-facing "No PDF was created" text.
- Root-caused a "still getting the same error" report: the user's already-open desktop window had been launched *before* the provider.py fix landed, so it was still running old in-memory code (Python doesn't hot-reload). Killed the stale process, relaunched fresh, re-verified — confirmed working.
- Icon work: rebuilt the app's taskbar/window icon (`assets/almond-desktop-icon.svg` → regenerated PNGs/`.ico`) to match the flat nut-glyph style used for the in-app bot icons instead of the old gradient leaf mark (fixed an XML bug along the way — `--` inside an SVG comment silently breaks the whole parse). Fixed the Desktop shortcut, which was pointing at a different, older icon file (`almond-desktop-bot.ico`) — repointed to the current one. Chased a real blur bug in the `.ico` build (a Pillow `sizes=` kwarg quirk) and found Explorer's icon-cache *database* (not just a live restart) needed a full purge for the desktop shortcut to actually show the fix — taskbar was never affected since it reads the icon live from the file every time, never cached.
- Regression caught mid-session: earlier badge cleanup removed General Chat's "Coming Soon" badge, breaking a test that documents it as a deliberate product decision. Confirmed with user and reverted — General Chat keeps its own badge (sidebar row + welcome-hero heading) independent of any quick-action card, since Calculator/Drafting/Document are all now shipped features with no badge of their own.
- Full suite: 323 passed throughout, only the 2 known pre-existing/unrelated `test_developer_services.py` mock failures (no `__iter__` on `FakeResponse` against the real streaming path) remain.
