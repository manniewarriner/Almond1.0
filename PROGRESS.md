# Almond Desktop — Current Checkpoint

## Codex - 2026-09-16 - Icon geometry checkpoint

- **Goal and success criteria:** Fix clipping and uneven whitespace around the Almond Financial brand mark plus every nut SVG, verify standalone rendering at actual UI sizes, preserve existing QLabel dimensions, and deliver everything to `claude/laughing-fermi-rkr3l4`.
- **Code snapshot:** Work started from remote commit `8c16e5a` in isolated worktree `C:\Users\manni\Almond1.0-codex`; this protected unrelated, overlapping edits already present in the canonical `C:\Users\manni\Almond1.0` main worktree.
- **Brand mark correction:** Measured combined transformed path bounds as approximately `x=56.0..144.9`, `y=41.7..151.0`; `src/almond_ai/desktop/icons.py` now uses normalized `viewBox="36 32 129 129"` and removes the unsafe outer `translate(16,10) scale(1.14)` transform.
- **Nut icon correction:** Recentered per-icon viewBoxes in `src/almond_ai/desktop/assets/icons/{general,walnut,hazelnut,almond,pistachio}.svg`; no shape paths, colours, or icon identities changed.
- **UI scope preserved:** `src/almond_ai/desktop/main_window.py` and `src/almond_ai/desktop/widgets/sidebar.py` remain untouched; standalone coverage explicitly checks 44px quick-action-card icons, 28px sidebar-row icons, the 46px sidebar brand mark, and the 128px welcome mark.
- **Verification:** Added `tests/test_desktop_icons.py`; all 12 standalone alpha-bound checks pass and Ruff passes. Post-fix margins are balanced within one logical pixel and every rendered shape remains clear of every canvas edge.
- **Known unrelated blocker and delivery:** Broader `tests/test_desktop.py` collection remains blocked by pre-existing `SyntaxError: unmatched ')'` at `app/local_model.py:129`; icon-specific verification is green. This checkpoint and icon changes are delivered together under commit subject `Fix desktop icon clipping and margins` without including main-worktree edits.

- **Goal and constraints:** The canonical project is `C:\Users\manni\Almond1.0` (keep cwd there for launches/tests). The current priority is the staff-facing Almond Desktop app: speed up Drafting and Document processing while leaving General Chat for later. Claude is concurrently working on the UI, so recent performance/model changes were confined to backend, worker, test, script, and documentation files; do not alter desktop design unless explicitly requested. Almond remains local-first and must never trade, move money, send messages, modify client records, access production, execute arbitrary shell/SQL, or use real client data.

- **Desktop design and reliability work completed:** The welcome logo was lowered to sit immediately above “A Quilter partner”; Settings privacy copy was made endpoint-aware; the duplicate Create PDF button beside the chat bar was removed while the orange welcome-panel button remains. PDF progress/results now survive navigation, excerpt ranges appear in the PDF/result/audit record, closing waits responsively for active Qt workers, and chat labels render text as plain text. Key files include `src/almond_ai/desktop/main_window.py`, `widgets/pdf_options_dialog.py`, `widgets/chat_view.py`, `workers.py`, and `widgets/settings_view.py`. General Chat and Research deliberately retain “Coming Soon.”

- **PDF speed work completed:** PDF extraction now defaults to local `pypdfium2==5.13.0` (Apache-2.0/BSD-3-Clause) with pypdf retained for diagnostics/output verification. The extraction cache uses immutable JSON payloads, at most 8 entries and 32 MiB, keyed by path/mtime/size/page range/backend. Desktop offers entire document, first 25, first 50, and custom ranges plus Streaming PDF numbering (`Page X`) to avoid ReportLab page replay. On the 1,174-page public sample, uncached extraction measured 11.752s with PDFium versus 181.546s with pypdf and 99.95% aggregate word/numeric-token overlap; full deterministic conversion still took 219.05s because ReportLab rendering became the dominant cost. See `app/documents/content_extract.py`, `app/pdf/{service.py,renderer.py}`, `docs/PDF_PERFORMANCE.md`, and `scripts/benchmark_pdf_{extraction,conversion}.py`.

- **Drafting bot acceleration completed and reviewed:** Drafting alone uses a compact No-Think MiniCPM5 profile in `src/almond_ai/core/drafting.py`; General Chat payload/history/settings are unchanged. It uses a 3,000-byte total prompt budget, 512-token output cap, temperature 0, seed 42, llama.cpp prompt caching, concise UK-email instructions, artifact cleanup that preserves figures/list indentation, and explicit errors instead of silent truncation. Fresh guided drafts omit previous clients; revisions retain the current draft’s original constraints. A synthetic live run produced first text in 5.422s and completed a usable 43-word draft in 10.867s; the uncapped legacy request exceeded 1,300 tokens without finishing and was interrupted. Review fixes covered nested lists, revision context, startup-error delivery, and output-limit handling; 61 focused tests passed. See `src/almond_ai/models/provider.py`, `src/almond_ai/core/application.py`, `tests/test_drafting_speed.py`, `docs/DRAFTING_PERFORMANCE.md`, and `scripts/benchmark_drafting.py`.

- **Document bot now shares the same loaded model in an isolated session:** Desktop Create PDF calls `AlmondCore.create_pdf(..., ai_format=True)` through `PdfCreateWorker`; it reuses the single MiniCPM5 provider but sends fresh document-only batches, never Drafting or General Chat history. `src/almond_ai/core/document_formatting.py` asks the model only to classify bounded candidate IDs (maximum 16 candidates/1,900 bytes per call) as headings or explicit lists using No-Think, JSON output, temperature 0, seed 42, prompt caching, and a 512-token cap. Rendered wording always comes from extracted source blocks; invalid JSON, invented/wrong IDs, model failure, remote endpoints, nested lists, and nonstandard numbering fail or preserve source text rather than silently rewriting it. The deterministic low-level PDF service/terminal command remain opt-in-free; Desktop enables AI formatting by default.

- **Verification state:** A live synthetic MiniCPM5 → branded PDF smoke test (`.\.venv\Scripts\python.exe scripts\verify_document_model.py`) completed in 6.917s, produced one verified page, and preserved `1,234.56`, `-12.5%`, “not confirmed,” and list text. The main document-focused suite passed 76 tests, the final formatting/core review passed 26 tests, the earlier broader relevant PDF/desktop/core suite passed 94 tests, changed files passed Ruff lint/format checks, and `pip check` reported no broken requirements. A full repository pytest run previously stalled around 50% in the repo’s known slow integration section, so do not claim the entire suite passes.

- **Next review items and tooling:** Restart Almond Desktop before manual review so the running process loads the new backend. Test Create PDF with synthetic short, multi-page, malformed, list-heavy, and page-range inputs, then benchmark AI-assisted formatting on a representative long document; large files can require many sequential MiniCPM5 batches and may be slower than deterministic conversion. If latency is excessive, preserve AI formatting but reduce safe heading candidates/batches or add an explicit deterministic fallback—do not let the model rewrite source facts. Headroom was not installed: no eligible Headroom package exists in the Codex plugin catalog, and the matching open-source account router documents monitoring/manual selection only on Windows—not the requested automatic handoff. Keep the two-package `app/` + `src/almond_ai/` merge deferred; it remains a separate, riskier cleanup.
